in:
    type: mysql
    host: localhost
    user: root
    password: unkoburiburi
    database: odds
    query: |
        SELECT * FROM main

out:
    type: elasticsearch
    index: test
    index_type: embulk
    nodes:
    - host: localhost